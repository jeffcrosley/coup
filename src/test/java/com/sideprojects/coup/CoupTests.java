package com.sideprojects.coup;

import java.util.ArrayList;
import java.util.List;

import org.junit.Assert;
import org.junit.FixMethodOrder;
import org.junit.Test;
import org.junit.runners.MethodSorters;

@FixMethodOrder(MethodSorters.NAME_ASCENDING)
public class CoupTests {
	
	/*
	@Test
	public void createPlayers() {
		// TEST CASE: 3 PLAYER GAME, NAMES CURLY, MOE, AND LARRY
		Assert.assertEquals(3, Coup.createPlayers().size());
		Assert.assertTrue(Coup.createPlayers().contains("Curly"));
		Assert.assertTrue(Coup.createPlayers().contains("Moe"));
		Assert.assertTrue(Coup.createPlayers().contains("Larry"));
	}
	*/
	
}
